<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.dulred.pmsbackend.repository.TransferRepository">

    <select id="getBeforePost" resultType="Transfer" parameterType="Integer">
        select s.sname as sname,p.id as beforepost_id, p.pname as beforepost_name
        from staff s,post p where s.id = #{id} and s.post_id = p.id
    </select>

</mapper>